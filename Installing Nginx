pipeline {
      agent any

    stages {
        stage ('install Nginx') {
            steps {
                sh """
                sudo apt update 
                sudo apt install nginx -y
                sudo systemctl stop nginx
                sudo systemctl start nginx
                """
            }
        }
    }
}